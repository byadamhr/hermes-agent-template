starlette>=0.40.0
uvicorn>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9
httpx>=0.27.0
# websockets is the upstream client our reverse proxy uses to connect to
# hermes's /api/pty, /api/ws, /api/events endpoints. httpx does not speak
# WebSockets, so we need a dedicated client library.
websockets>=12.0
# hermes-agent imports ruamel.yaml (hermes_yaml.py) and python-dotenv at
# runtime, but neither is declared as a direct dependency in hermes-agent's
# own pyproject.toml — it expects them to be present transitively or
# preinstalled. Without them, `hermes dashboard` crashes on boot with
# ModuleNotFoundError. Keep these permanently, independent of any
# honcho-ai version pinning.
ruamel.yaml>=0.18.0
python-dotenv>=0.19.0
